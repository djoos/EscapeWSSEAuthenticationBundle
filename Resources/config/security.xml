<?xml version="1.0" ?>

<container xmlns="http://symfony.com/schema/dic/services"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://symfony.com/schema/dic/services http://symfony.com/schema/dic/services/services-1.0.xsd">

    <services>
        <service id="escape_wsse.authentication" class="%escape_wsse.authentication.provider.class%" public="false">
            <argument /> <!-- Provider-shared Key -->
            <argument /> <!-- noncedir -->
            <argument /> <!-- lifetime -->
        </service>

        <service id="escape_wsse.security.authentication.listener"
                 class="%escape_wsse.authentication.listener.class%"
                 parent="security.authentication.listener.abstract"
                 abstract="true">
        </service>

    </services>
</container>
